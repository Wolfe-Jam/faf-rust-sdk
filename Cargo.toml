[package]
name = "faf-rust-sdk"
version = "2.0.1"
edition = "2024"
rust-version = "1.85"
authors = ["wolfejam <wolfejam@faf.one>"]
description = "Persistent Project Context for Rust. IANA-registered .faf format, parse, validate, score, FAFb binary."
license = "MIT"
repository = "https://github.com/Wolfe-Jam/faf-rust-sdk"
homepage = "https://faf.one"
documentation = "https://docs.rs/faf-rust-sdk"
readme = "README.md"
keywords = ["faf", "ai-context", "yaml", "mcp", "grok"]
categories = ["parsing", "data-structures"]
exclude = ["target/", ".git/", "*.lock"]

[features]
default = []
axum = ["dep:axum", "dep:tower", "dep:http"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml_ng = "0.10"
thiserror = "1.0"
byteorder = "1.5"
crc32fast = "1.3"
axum = { version = "0.8", optional = true }
tower = { version = "0.5", optional = true }
http = { version = "1.0", optional = true }

[dev-dependencies]
criterion = "0.5"
tempfile = "3.10"
tokio = { version = "1", features = ["full"] }
axum = "0.8"
tower = { version = "0.5", features = ["util"] }
